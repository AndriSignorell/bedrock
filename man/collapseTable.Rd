% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/collapseTable.R
\name{collapseTable}
\alias{collapseTable}
\title{Collapse table dimensions by remapping factor levels}
\usage{
collapseTable(x, ..., strict = TRUE)
}
\arguments{
\item{x}{A \code{table} or \code{ftable} object with named dimensions.}

\item{...}{Named or unnamed mapping vectors, one per dimension to collapse.
Each mapping vector must have length equal to the number of levels of the
corresponding dimension. Element \emph{i} defines the new label for the
\emph{i}-th original level. Repeated values in a mapping vector cause the
corresponding levels to be merged (collapsed).

\strong{Argument matching:}
\itemize{
\item \strong{Named arguments:} Matched to dimensions by name, e.g.
\code{age = c("young", "adult", "adult")}.
\item \strong{Unnamed arguments:} Assigned to dimensions in order.
In \code{strict = TRUE} mode, this triggers a warning.
\item \strong{Mixed usage:} Named arguments are matched first; remaining
unnamed arguments are assigned to the remaining dimensions in order.
}

\strong{Constraints:}
\itemize{
\item Each dimension may be specified at most once.
\item Mapping vectors must have the same length as the number of levels
of the corresponding dimension.
\item \code{NA} values in mapping vectors are not allowed.
}

\strong{Level ordering:}
The order of the resulting levels follows the first occurrence of each
value in the mapping vector.}

\item{strict}{Logical (default \code{TRUE}). Controls error handling:
\itemize{
\item If \code{TRUE}, unknown dimension names result in an error and
positional assignment of unnamed arguments produces a warning.
\item If \code{FALSE}, unknown dimensions produce a warning (and are
ignored), and positional assignment is silent.
}}
}
\value{
A \code{table} with the same dimensions as \code{x}, but with
collapsed levels and aggregated frequencies. Cells corresponding to
level combinations that are absent after collapsing are returned as
\code{0}.
}
\description{
Aggregates a \code{table} or \code{ftable} object by reassigning the levels
of one or more dimensions according to user-supplied mappings, and summing
the frequencies within each resulting level combination.
}
\details{
The function operates by converting the table to a long-format data frame,
applying the level mappings, aggregating frequencies, and reconstructing a
table with consistent level ordering.

Factor levels are reassigned explicitly via integer indexing rather than
modifying \code{levels()}, ensuring deterministic and robust behaviour.
}
\examples{
# Create a simple 3x3 table
m <- matrix(1:9, 3, 3,
            dimnames = list(
              age = c("0-17", "18-64", "65+"),
              edu = c("low", "mid", "high")
            ))
tab <- as.table(m)

# Collapse age into two groups
collapseTable(tab, age = c("minor", "adult", "adult"))

# Collapse both dimensions
collapseTable(
  tab,
  age = c("minor", "adult", "adult"),
  edu = c("low", "high", "high")
)

# Positional (unnamed) mapping
collapseTable(tab, c("minor", "adult", "adult"))

}
\seealso{
\code{\link{xtabs}}, \code{\link{as.data.frame.table}}

Other table.utils: 
\code{\link{dummy}()},
\code{\link{multMerge}()},
\code{\link{printCharMatrix}()}
}
\concept{factor-handling}
\concept{table-manipulation}
\concept{table.utils}
