% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rBetaShape.R
\name{rBetaShape}
\alias{rBetaShape}
\title{Generate Beta-Distributed Random Values by Shape}
\usage{
rBetaShape(
  n,
  shape = c("norm", "left", "right", "unif", "u", "j", "inv-j"),
  bounds = c(0, 1)
)
}
\arguments{
\item{n}{non-negative integer giving the number of values to generate.}

\item{shape}{distributional shape: either one of the predefined names
listed under \strong{Details}, or a numeric vector of length 2 giving
\code{shape1} and \code{shape2} directly.}

\item{bounds}{numeric vector containing the lower and upper bound.}
}
\value{
a numeric vector of length \code{n} with values within \code{bounds}.
}
\description{
Generates beta-distributed random values using predefined distributional
shapes and transforms them to a specified interval.
}
\details{
The following predefined shapes and beta parameters are available:

\tabular{lrrl}{
Shape \tab \code{shape1} \tab \code{shape2} \tab Description \cr
\code{"norm"}  \tab 5.0 \tab 5.0 \tab symmetric and bell-shaped \cr
\code{"left"}  \tab 5.0 \tab 2.0 \tab left-skewed with values concentrated near the upper bound \cr
\code{"right"} \tab 2.0 \tab 5.0 \tab right-skewed with values concentrated near the lower bound \cr
\code{"unif"}  \tab 1.0 \tab 1.0 \tab uniform \cr
\code{"u"}     \tab 0.5 \tab 0.5 \tab U-shaped with values concentrated near both bounds \cr
\code{"j"}     \tab 2.0 \tab 0.5 \tab J-shaped with values concentrated near the upper bound \cr
\code{"inv-j"} \tab 0.5 \tab 2.0 \tab inverse J-shaped with values concentrated near the lower bound
}

Note that \code{"left"} and \code{"right"} name the direction of the \emph{skew}, i.e.
of the long tail, so \code{"right"} places the bulk of the values near the
lower bound. This is the standard convention, but it is the opposite of
what the names suggest at first reading - and unrelated to the meaning
of \code{"left"}/\code{"right"} in the \code{sides} argument of the interval functions,
where they name the side carrying the finite bound.

The \code{"norm"} shape is symmetric and bell-shaped but is not a normal
distribution. Unlike the normal distribution, all generated values are
bounded.

Values from the standard beta distribution on the interval \eqn{[0,1]}
are transformed to the interval specified by \code{bounds} as

\deqn{a + (b-a)X}{a + (b - a) * X}

where \eqn{a} and \eqn{b} are the lower and upper bounds, respectively.
}
\section{Random number generation}{

The values are drawn with \code{\link[stats:rbeta]{stats::rbeta()}} and therefore depend on the
state of R's global random number generator. No seed is set internally;
call \code{\link[base:set.seed]{base::set.seed()}} beforehand, or wrap the call in
\code{\link[=withSeed]{withSeed()}}, for reproducible results.
}

\examples{
set.seed(42)

x <- rBetaShape(
  1000,
  shape = "right",
  bounds = c(10, 90)
)

summary(x)
range(x)

# shape parameters can also be given directly
rBetaShape(5, shape = c(3, 1.5), bounds = c(0, 100))

}
\seealso{
\code{\link[stats:rbeta]{stats::rbeta()}}, \code{\link[stats:runif]{stats::runif()}}

Other random.numbers:
\code{\link[=rSum21]{rSum21()}}
}
\concept{random.numbers}
\concept{sampling}
