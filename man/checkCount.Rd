% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/checks.R
\name{checkCount}
\alias{checkCount}
\title{Validate a Count}
\usage{
checkCount(x, min = 0L, name = deparse(substitute(x)))
}
\arguments{
\item{x}{the value to check.}

\item{min}{the smallest admissible value, \code{0} by default. Pass
\code{1} for the arguments that must be positive, e.g. a width or a
number of replicates.}

\item{name}{the argument name to use in the message. Defaults to the
expression that was passed.}
}
\value{
\code{x}, invisibly.
}
\description{
Checks that an argument is a single finite integer, not smaller than
\code{min}. Meant for the many size arguments in the suite - \code{digits},
\code{sep}, \code{width}, \code{nPerm}, \code{R} and the like - which
are conceptually counts rather than numbers and were previously
spelled out by hand wherever they occur.
}
\details{
A whole number stored as a double is accepted, as that is what
arithmetic on integers produces and what a user typing \code{2} supplies.
\code{TRUE} is not, although it would survive \code{as.integer()}: a flag
that reaches a count argument is a mistake, not a shorthand for one.

The order of the tests is the same as in \code{\link[=checkConfLevel]{checkConfLevel()}}, length
first, then type, then value, so that the message names the argument
rather than the condition that failed.
}
\examples{
sep <- 2
checkCount(sep)

width <- 80
checkCount(width, min = 1)

\dontrun{
checkCount(1.5)                # not a whole number
checkCount(-1)                 # below the default minimum
checkCount(TRUE)               # a flag is not a count
}

}
\seealso{
\code{\link[=checkConfLevel]{checkConfLevel()}}, \code{\link[=checkFlag]{checkFlag()}}, \code{\link[=checkString]{checkString()}}
}
