% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/isWholeLike.R
\name{isWholeLike}
\alias{isWholeLike}
\title{Test Whether Values Are (Nearly) Whole Numbers}
\usage{
isWholeLike(
  x,
  all = TRUE,
  isNonNegative = FALSE,
  tol = sqrt(.Machine$double.eps),
  na.rm = FALSE
)
}
\arguments{
\item{x}{a numeric, integer, or complex vector}

\item{all}{logical. If \code{TRUE} (default), returns a single logical
indicating whether all elements are whole-like. If \code{FALSE},
returns a logical vector of the same length as \code{x}.}

\item{isNonNegative}{logical. If \code{TRUE}, additionally requires values
to be non-negative.}

\item{tol}{numerical tolerance for comparing to the nearest integer.
Default is \code{sqrt(.Machine$double.eps)}.}

\item{na.rm}{logical. If \code{TRUE}, missing values are removed before
testing. If \code{FALSE} (default) and \code{x} contains \code{NA},
the result is \code{FALSE}.}
}
\value{
If \code{all = TRUE}, a single logical value.
If \code{all = FALSE}, a logical vector.
}
\description{
Checks whether values are integer-like within a numerical tolerance.
Works for numeric, integer, and complex vectors.
}
\details{
A value is considered whole-like if the absolute difference between
the value and its nearest integer is smaller than \code{tol}.

For complex numbers, both real and imaginary parts must be whole-like;
with \code{isNonNegative = TRUE}, both parts must additionally be
non-negative.
}
\examples{
isWholeLike(c(1, 2, 3))
isWholeLike(c(1, 2.0000001), tol = 1e-6)
isWholeLike(c(1, 2.5), all = FALSE)
isWholeLike(c(1, -2), isNonNegative = TRUE)
isWholeLike(1:5 + 0i)

}
\seealso{
Other data.predicate:
\code{\link[=flags]{flags()}},
\code{\link[=isDichotomous]{isDichotomous()}},
\code{\link[=isEuclid]{isEuclid()}},
\code{\link[=isLowCardinality]{isLowCardinality()}},
\code{\link[=isNumeric]{isNumeric()}},
\code{\link[=isZero]{isZero()}},
\code{\link[=nUnique]{nUnique()}}
}
\concept{data-inspection}
\concept{data.predicate}
\concept{type-test}
