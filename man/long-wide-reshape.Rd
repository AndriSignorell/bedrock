% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/toLongWide.R
\name{long-wide-reshape}
\alias{long-wide-reshape}
\alias{toLong}
\alias{toWide}
\title{Reshape Between Long and Wide Format}
\usage{
toLong(x, varNames = NULL, includeRowNames = FALSE)

toWide(x, groups, by = NULL, varNames = NULL)
}
\arguments{
\item{x}{object to reshape. For \code{toLong()}, a matrix, table, data frame, or
list. For \code{toWide()}, a vector.}

\item{varNames}{optional character vector of column names for the result}

\item{includeRowNames}{logical. If \code{TRUE}, append a column containing the row
names of \code{x} when reshaping to long format.}

\item{groups}{grouping vector used to define the columns in the wide result}

\item{by}{optional vector used to align values row-wise when reshaping to
wide format. If \code{NULL}, values are aligned by their order within each group.}
}
\value{
A reshaped object of class \code{data.frame}.
}
\description{
Reshape data between long and wide format using a grouping variable.
}
\details{
\code{toLong()} expects \code{x} to be a matrix, table, data frame, or list and
reshapes it to a long data frame representation. \code{toWide()} expects a vector
\code{x} and a grouping vector \code{groups}, and reshapes the values into one column
per group.
}
\examples{
d.x <- read.table(header = TRUE, text = "
AA BB CC DD EE FF GG
7.9 18.1 13.3 6.2 9.3 8.3 10.6
9.8 14.0 13.6 7.9 2.9 9.1 13.0
6.4 17.4 16.0 10.9 8.6 11.7 17.5
")

toLong(d.x)

# to wide by row order
toWide(PlantGrowth$weight, PlantGrowth$group)

# to wide aligned by key
set.seed(41)
PlantGrowth$nr <- c(sample(12, 10), sample(12, 10), sample(12, 10))
toWide(PlantGrowth$weight, PlantGrowth$group, by = PlantGrowth$nr)

}
\seealso{
\code{reshape}, \code{stack}, \code{unstack}

Other data.reshape:
\code{\link[=collapseTable]{collapseTable()}},
\code{\link[=splitAt]{splitAt()}},
\code{\link[=splitX]{splitX()}},
\code{\link[=untable]{untable()}}
}
\concept{data.reshape}
\concept{reshape}
\concept{table}
