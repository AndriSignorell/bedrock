% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/appendEnum.R
\name{appendEnum}
\alias{appendEnum}
\title{Add an Enumeration Column}
\usage{
appendEnum(
  x,
  type = c("roman-lcase", "roman-ucase", "arabic"),
  suffix = ". ",
  startWith = 1L,
  after = 0L,
  colName = NULL
)
}
\arguments{
\item{x}{a data.frame or matrix (vectors are coerced via \code{matrix()}).}

\item{type}{enumeration style: \code{"roman-lcase"}, \code{"roman-ucase"}
or \code{"arabic"}.}

\item{suffix}{text appended to each enumeration label.}

\item{startWith}{first enumeration index.}

\item{after}{position after which the column is inserted (see
\code{\link{appendX}}); default \code{0L} prepends it.}

\item{colName}{optional name for the new column; \code{NULL} (default)
leaves it unnamed for matrices, while for data frames a default name
(\code{"V1"}) is used.}
}
\value{
\code{x} with an additional enumeration column.
}
\description{
Prepends (or inserts) a column of enumeration labels -- lowercase or
uppercase Roman numerals, or Arabic numbers.
}
\examples{
d <- data.frame(x = 1:3, y = c("a", "b", "c"))
appendEnum(d)

appendEnum(d, type = "arabic", suffix = ") ")

# insert after the first column instead of prepending
appendEnum(d, after = 1L, colName = "no")

}
\seealso{
\code{\link{append}}

Other data.append:
\code{\link[=appendRowNames]{appendRowNames()}},
\code{\link[=appendX]{appendX()}},
\code{\link[=multMerge]{multMerge()}}
}
\concept{append}
\concept{data.append}
\concept{table}
