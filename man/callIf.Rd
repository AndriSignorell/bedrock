% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/callIf.R
\name{callIf}
\alias{callIf}
\title{Conditionally Call a Function}
\usage{
callIf(fun, arg, defaults = NULL, forbidden = NULL, warn = TRUE)
}
\arguments{
\item{fun}{a function to be called}

\item{arg}{controls whether and how \code{fun} is called:
\itemize{
\item \code{FALSE}, \code{NULL}, or \code{NA}: \code{fun} is not called
and \code{NULL} is returned invisibly.
\item \code{TRUE}: \code{fun} is called with \code{defaults} (if provided),
or with no arguments.
\item A fully named list: \code{fun} is called with the list elements as
arguments. If \code{defaults} is provided, it is merged with
\code{arg}, where elements of \code{arg} override those in
\code{defaults}.
}}

\item{defaults}{a named list of default arguments passed to \code{fun} when
\code{arg = TRUE}, or used as a base when \code{arg} is a list.
Default is \code{NULL}.}

\item{forbidden}{optional character vector of argument names that are not
allowed. If any of these appear in \code{arg}, they are removed before
calling \code{fun}. A warning is issued unless \code{warn = FALSE}.}

\item{warn}{logical. If \code{TRUE} (default), a warning is issued when
forbidden arguments are removed.}
}
\value{
Returns the result of \code{fun(...)} if called. If \code{arg} is
\code{FALSE}, \code{NULL}, or \code{NA}, returns \code{NULL} invisibly.
}
\description{
Conditionally evaluate a function depending on the value of an argument.
This is a convenient helper for optional features such as plotting,
logging, or callbacks, where the user can enable, disable, or parameterize
a function call via a single argument.
}
\details{
This function implements a flexible pattern for optional function calls:

\itemize{
\item Enable/disable behavior with \code{TRUE}/\code{FALSE}
\item Customize behavior with a list of arguments
\item Provide safe defaults and restrict certain arguments
}

When merging \code{defaults} and \code{arg}, user-supplied arguments take
precedence. Unlike \code{\link{modifyList}}, elements with the value
\code{NULL} are preserved and passed on to \code{fun} (so that an explicit
\code{NULL} can be used to reset an argument).
}
\examples{
# Simple usage: skip
callIf(message, FALSE)

# Call with defaults
callIf(message, TRUE, defaults = list("Hello world"))

# Call with explicit arguments
callIf(message, list(x = "Hello from callIf"))

# With defaults + override
callIf(plot, list(x = 1:5),
       defaults = list(y = 1:5, type = "l"))

# Forbid arguments
callIf(plot,
       list(x = 1:5, y = 1:5, col = "red"),
       forbidden = "col")

# Typical use case: optional plotting
x <- 1:10
y <- x^2
callIf(plot, TRUE, defaults = list(x, y))

}
\seealso{
Other pkg.args:
\code{\link[=extractArgs]{extractArgs()}},
\code{\link[=getDotsArg]{getDotsArg()}},
\code{\link[=mergeArgs]{mergeArgs()}},
\code{\link[=recycle]{recycle()}}
}
\concept{conditional-execution}
\concept{pkg.args}
\concept{programming}
