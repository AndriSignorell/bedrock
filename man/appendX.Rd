% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/appendX.R
\name{appendX}
\alias{appendX}
\alias{appendX.default}
\alias{appendX.matrix}
\alias{appendX.data.frame}
\alias{appendX.TOne}
\title{Append Elements to Objects}
\usage{
appendX(x, values, after = NULL, ...)

\method{appendX}{default}(x, values, after = NULL, ...)

\method{appendX}{matrix}(x, values, after = NULL, rows = FALSE, newNames = NULL, ...)

\method{appendX}{data.frame}(x, values, after = NULL, rows = FALSE, newNames = NULL, ...)

\method{appendX}{TOne}(x, values, after = NULL, rows = TRUE, newNames = NULL, ...)
}
\arguments{
\item{x}{object to which values are appended.}

\item{values}{values to insert into \code{x}. For matrices with
\code{rows = TRUE}, values are read row by row.}

\item{after}{position after which to insert. If \code{NULL}, values are appended
at the end. Use \code{0} to prepend.}

\item{...}{additional arguments.}

\item{rows}{logical; if TRUE, insert rows instead of columns. Ignored for
vectors. Note that the method for \code{TOne} objects defaults to
\code{rows = TRUE}, as appending rows is the typical use case there.}

\item{newNames}{optional names for the inserted elements: column names when
inserting columns, row names when inserting rows. When inserting a column
into a data.frame without giving \code{newNames}, default names
(\code{"V1"}, \code{"V2"}, ...) are used.}
}
\value{
object of the same class as \code{x}.
}
\description{
Generic function to append elements to vectors, matrices, and data frames.
}
\examples{
# vectors
appendX(1:5, 99, after = 2)

# matrices: insert a column / a row
m <- matrix(1:6, nrow = 2,
            dimnames = list(c("r1", "r2"), c("a", "b", "c")))
appendX(m, c(9, 9), after = 1, newNames = "z")
appendX(m, 7:9, after = 1, rows = TRUE, newNames = "r1b")

# data frames: insert a column / a row
d <- data.frame(a = 1:3, b = 4:6)
appendX(d, 7:9, after = 1, newNames = "z")
appendX(d, list(a = 99, b = 88), after = 0, rows = TRUE)

}
\seealso{
\code{\link{append}}

Other data.append:
\code{\link[=appendEnum]{appendEnum()}},
\code{\link[=appendRowNames]{appendRowNames()}},
\code{\link[=multMerge]{multMerge()}}
}
\concept{append}
\concept{data.append}
\concept{table}
