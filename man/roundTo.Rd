% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/roundTo.R
\name{roundTo}
\alias{roundTo}
\title{Round to a Multiple}
\usage{
roundTo(x, multiple = 1, FUN = round)
}
\arguments{
\item{x}{numeric. The values to be rounded.}

\item{multiple}{numeric. The step width to whose multiples the values are to
be rounded, defaults to \code{1}. Must be finite and positive and either a single
value or as long as \code{x}.}

\item{FUN}{the rounding function applied to \code{x / multiple}. Typically one of
\code{\link[=round]{round()}} (default), \code{\link[=trunc]{trunc()}}, \code{\link[=ceiling]{ceiling()}} or \code{\link[=floor]{floor()}}. Other functions
accepting and returning a numeric vector can be used as well.}
}
\value{
a numeric vector of the rounded values, as long as \code{x}. \code{NA}s in \code{x}
are returned as \code{NA}.
}
\description{
Rounds the values of a numeric vector to the nearest multiple of a given
step width. Where \code{\link[=round]{round()}} is tied to multiples of a power of ten,
\code{roundTo()} accepts an arbitrary step, so that prices can be rounded to the
nearest 5 cents, durations to the nearest quarter of an hour or axis limits
to the nearest 250. The direction of the rounding is controlled by \code{FUN},
which allows rounding to the nearest, upwards, downwards or towards zero
with the same interface.
}
\details{
There are several functions in base R to convert to integers. \code{\link[=round]{round()}}
rounds to the nearest integer or to any number of digits. Using a negative
number of digits rounds to a power of ten, so that \code{round(x, -3)} rounds to
thousands. Each of \code{\link[=trunc]{trunc()}}, \code{\link[=floor]{floor()}} and \code{\link[=ceiling]{ceiling()}} rounds in a fixed
direction, towards zero, down and up respectively. \code{\link[=round]{round()}} is documented
to round half to even, so \code{round(2.5)} is \code{2}.

\code{roundTo()} evaluates \code{FUN(x / multiple) * multiple}. With the default
\code{FUN = round} a value lying exactly halfway between two multiples is
therefore rounded to the one with the even quotient: \code{roundTo(1, 2)} is \code{0}
and \code{roundTo(3, 2)} is \code{4}. Setting \code{FUN = ceiling} always rounds up,
\code{FUN = floor} always rounds down and \code{FUN = trunc} always towards zero (see
the examples for a comparison).

Ties are rare in practice, as most decimal fractions have no exact binary
representation. \code{1.3 / 0.2} is marginally smaller than \code{6.5} in double
precision, so \code{roundTo(1.3, 0.2)} returns \code{1.2} and not the \code{1.4} that the
rule for ties would suggest. Results for a fractional \code{multiple} are
likewise only accurate to within representation error, which is why
\code{roundTo(x, 0.05)} may still print more than two decimal places.

A single \code{multiple} is used for all the values in \code{x}. A vector of step
widths is applied elementwise and must then be exactly as long as \code{x}, so
that a length mismatch is reported as an error instead of being recycled
silently.
}
\examples{

roundTo(10, 3)     # rounds 10 to the nearest multiple of 3 (9)
roundTo(-10, 3)    # rounds -10 to the nearest multiple of 3 (-9)

roundTo(1.3, 0.2)  # rounds 1.3 to the nearest multiple of 0.2 (1.2)
roundTo(-1.3, 0.2) # rounds -1.3 to the nearest multiple of 0.2 (-1.2)

# prices to the nearest 5 cents
roundTo(c(1.02, 1.03, 12.375), 0.05)

# a step width for every value
roundTo(c(1.23, 123, 1234), c(0.05, 10, 100))

# any other length is an error, the values are not recycled
try(roundTo(1:6, c(2, 3)))

# round down
roundTo(c(1, -1) * 1.2335, 0.05, floor)
roundTo(c(1, -1) * 1233.5, 100, floor)

# round up
roundTo(c(1, -1) * 1.2335, 0.05, ceiling)
roundTo(c(1, -1) * 1233.5, 100, ceiling)

# round towards zero
roundTo(c(1, -1) * 1.2335, 0.05, trunc)
roundTo(c(1, -1) * 1233.5, 100, trunc)

# the four directions side by side
x <- c(-1.5, -1.3, 1.3, 1.5)
cbind(x       = x,
      round   = roundTo(x, 0.2, FUN = round),
      trunc   = roundTo(x, 0.2, FUN = trunc),
      ceiling = roundTo(x, 0.2, FUN = ceiling),
      floor   = roundTo(x, 0.2, FUN = floor)
)

# note how the ties in the first column are resolved to even multiples
x <- -5:5
cbind(x       = x,
      round   = roundTo(x, 2, FUN = round),
      trunc   = roundTo(x, 2, FUN = trunc),
      ceiling = roundTo(x, 2, FUN = ceiling),
      floor   = roundTo(x, 2, FUN = floor)
)


}
\seealso{
\code{\link[=round]{round()}}, \code{\link[=trunc]{trunc()}}, \code{\link[=ceiling]{ceiling()}}, \code{\link[=floor]{floor()}}

Other math.basic:
\code{\link[=closest]{closest()}},
\code{\link[=crossProd]{crossProd()}},
\code{\link[=crossProdN]{crossProdN()}},
\code{\link[=dotProd]{dotProd()}},
\code{\link[=unirootAll]{unirootAll()}}
}
\concept{math.basic}
\concept{numerical-methods}
