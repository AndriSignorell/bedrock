% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/checks.R
\name{checkString}
\alias{checkString}
\title{Validate a Character String}
\usage{
checkString(x, name = deparse(substitute(x)))
}
\arguments{
\item{x}{the value to check.}

\item{name}{the argument name to use in the message. Defaults to the
expression that was passed.}
}
\value{
\code{x}, invisibly.
}
\description{
Checks that an argument is a single non-missing character string.
Meant for the labelling arguments across the suite - \code{dataName},
captions, axis titles - where a vector or an \code{NA} would otherwise
travel unnoticed into printed output.
}
\details{
An optional argument that may also be \code{NULL} is guarded by the
caller, \code{if (!is.null(dataName)) checkString(dataName)}, rather than
by a further argument here: whether the absence of a label is
admissible is a decision of the function, not of the check.

The empty string is accepted. It is a legitimate label, and a caller
that needs a non-empty one says so itself.
}
\examples{
dataName <- "smoking by sex"
checkString(dataName)

\dontrun{
checkString(NA_character_)     # a missing label is not a label
checkString(c("a", "b"))       # length
checkString(42)                # type
}

}
\seealso{
\code{\link[=checkConfLevel]{checkConfLevel()}}, \code{\link[=checkFlag]{checkFlag()}}, \code{\link[=checkCount]{checkCount()}}
}
