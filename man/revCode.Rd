% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/revCode.R
\name{revCode}
\alias{revCode}
\title{Reverse Coding of Variables}
\usage{
revCode(x, min = NULL, max = NULL, na.rm = FALSE)
}
\arguments{
\item{x}{A vector (numeric, logical, or factor).}

\item{min}{Optional numeric minimum. Must be provided together with \code{max}.
If \code{NULL} (default), the observed minimum of \code{x} is used.}

\item{max}{Optional numeric maximum. Must be provided together with \code{min}.
If \code{NULL} (default), the observed maximum of \code{x} is used.}

\item{na.rm}{Logical; whether to ignore \code{NA}s when computing the range
(numeric only). If \code{FALSE} and \code{NA}s are present, a warning is
issued and \code{NA} is returned for all values. Default is \code{FALSE}.}
}
\value{
A vector of the same type and length as \code{x}, with reversed coding.
}
\description{
Reverses the coding of a vector. Supports numeric, logical, and factor inputs:
\itemize{
\item \strong{Numeric}: Transforms values using \code{min + max - x}
\item \strong{Logical}: Flips TRUE/FALSE
\item \strong{Factor}: Reverses the order of levels
}
}
\section{Errors}{

Throws an error if all values are \code{NA}, if only one of \code{min}/\code{max}
is provided, if \code{min > max}, or if \code{x} is not numeric, logical, or factor.
}

\examples{
# Numeric
revCode(c(1, 2, 3, 4, 5))

# Numeric with explicit range (e.g., Likert scale)
revCode(c(1, 2, 3, 4, 5), min = 1, max = 5)

# Numeric with NAs
revCode(c(1, 2, NA, 4, 5), na.rm = TRUE)

# Logical
revCode(c(TRUE, FALSE, TRUE))

# Factor
x <- factor(c("low", "medium", "high"), ordered = TRUE)
revCode(x)

}
\seealso{
Other data.manipulation: 
\code{\link{appendRowNames}()},
\code{\link{appendX}()},
\code{\link{as.array.xtabs}()},
\code{\link{columnWrap}()},
\code{\link{combLevels}()},
\code{\link{nf}()},
\code{\link{parseSASDatalines}()},
\code{\link{recodeX}()},
\code{\link{recycle}()},
\code{\link{revX}()},
\code{\link{setNamesX}()},
\code{\link{set_strip_attr}},
\code{\link{sortX}()},
\code{\link{splitX}()},
\code{\link{stringsAsFactors}()},
\code{\link{toBaseR}()},
\code{\link{tolong_towide}},
\code{\link{untable}()}
}
\concept{data-manipulation}
\concept{data.manipulation}
\concept{factor-handling}
