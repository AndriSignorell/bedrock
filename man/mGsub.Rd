% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mGsub.R
\name{mGsub}
\alias{mGsub}
\title{Multiple String Substitution}
\usage{
mGsub(x, patterns, replacements)
}
\arguments{
\item{x}{A character vector in which substitutions are performed.}

\item{patterns}{A character vector of substrings to search for
(\code{fixed = TRUE}).}

\item{replacements}{A character vector of replacement strings,
in the same order as \code{patterns}.}
}
\value{
A character vector of the same length as \code{x}.
}
\description{
Replaces multiple substrings in a character vector simultaneously,
avoiding the cascade problem where an earlier replacement becomes the
target of a later one. Internally uses temporary unique tokens as
an intermediate step.
}
\examples{
mGsub(c("foo bar", "bar foo"), c("foo", "bar"), c("bar", "foo"))
# [1] "bar foo" "foo bar"

# Without simultaneous replacement this would yield "foo foo"
# with sequential gsub().

x <- c("A", "B", "AB", "BA")
mGsub(x, patterns = c("A", "B"), replacements = c("BX", "CY"))

}
\seealso{
\code{\link{mReplace}} for exact whole-element replacement.

Other string:
\code{\link{char-ascii-conversion}}
}
\concept{string}
\concept{string-manipulation}
